---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "staging-econplaza-config-map"
  namespace: "default"
  labels:
    app: "econplaza"
data:
  HOSTNAMES: "staging.api.econplaza.bebbo.link"
---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "staging-econplaza"
  namespace: "default"
  labels:
    app: "staging-econplaza"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "staging-econplaza"
  template:
    metadata:
      labels:
        app: "staging-econplaza"
    spec:
      containers:
      - name: "staging-app"
        image: "<<DOCKER_IMAGE>>"
        ports:
        - containerPort: 80
        env:
        - name: "DB_PASSWORD"
          valueFrom:
            secretKeyRef:
              name: db.secret
              key: password
        - name: "DB_USERNAME"
          valueFrom:
            secretKeyRef:
              name: db.secret
              key: username
        - name: "DB_HOST"
          valueFrom:
            secretKeyRef:
              name: db.secret
              key: host
        - name: "DB_PORT"
          valueFrom:
            secretKeyRef:
              name: db.secret
              key: port
        - name: "DB_NAME"
          valueFrom:
            secretKeyRef:
              name: db.secret
              key: name
        - name: "HOSTNAMES"
          valueFrom:
            configMapKeyRef:
              key: "HOSTNAMES"
              name: "staging-econplaza-config-map"
        - name: "INSTANCE_IP"
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
---
apiVersion: "autoscaling/v2beta1"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "staging-econplaza-hpa-dtlm"
  namespace: "default"
  labels:
    app: "staging-econplaza"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "staging-econplaza"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: "Resource"
      resource:
        name: "cpu"
        targetAverageUtilization: 80