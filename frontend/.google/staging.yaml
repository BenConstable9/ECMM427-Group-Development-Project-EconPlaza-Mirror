---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "staging-econplaza-frontend"
  namespace: "default"
  labels:
    app: "staging-econplaza-frontend"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "staging-econplaza-frontend"
  template:
    metadata:
      labels:
        app: "staging-econplaza-frontend"
    spec:
      containers:
      - name: "app-sha256-1"
        image: "<<DOCKER_IMAGE>>"
        ports:
        - containerPort: 80
---
apiVersion: "autoscaling/v2beta1"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "staging-econplaza-frontend-hpa-dtlm"
  namespace: "default"
  labels:
    app: "staging-econplaza-frontend"
spec:
  scaleTargetRef:
    kind: "Deployment"
    name: "staging-econplaza-frontend"
    apiVersion: "apps/v1"
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: "Resource"
    resource:
      name: "cpu"
      targetAverageUtilization: 80